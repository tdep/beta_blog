# README

## Beta Blog

This is a demo site to accompany the Ruby on Rails Masterclass on Udemy.

To run:

Fork and clone the repo:
`git clone ...`
Install the gems:
`bundle install`
Start the server:
`bin/dev`